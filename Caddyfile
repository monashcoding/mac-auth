# Caddy reverse proxy for the MAC auth service.
# Automatic HTTPS via Let's Encrypt for auth.monashcoding.com. ACME_EMAIL comes from
# the environment (set in docker-compose from .env).

{
	email {$ACME_EMAIL}
}

auth.monashcoding.com {
	encode gzip
	reverse_proxy auth:3000
}
